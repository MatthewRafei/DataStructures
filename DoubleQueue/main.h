//void pop_front(); //Removes the first element from the deque.
//void pop_back(); //Removes the last element from the deque.
void empty(); //Checks whether the deque is empty or not.
void size(); //Determines the number of elements in the deque.
void clear(); // Remove all elements leave size of 0
void erase(); //Remove one or more elements from double qeue
void swap(); //Swap one or more values with given number
